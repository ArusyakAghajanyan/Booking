const store = {
   setItem(key, value) {      {
           localStorage.setItem(key, JSON.stringify(value));
       },
       getItem(key) {
          return 
    }
        
           
       }
   },
  
};

export default store;