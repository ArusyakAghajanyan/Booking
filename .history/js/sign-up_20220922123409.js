// Elements
const $loginTextInputs = document.querySelectorAll('.login__text-input');
console.log($loginTextInputs);

// Event listners
$loginTextInputs.forEach(($loginTextInput) => {
    $loginTextInput.addEventListener
})

//Data
const userData = {};