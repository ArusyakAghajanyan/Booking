const markupCreators = {
    playCard: (item) => {
        return `
         <div>
         <span>${item.name}</span>
         <span>${new}</span>
         
         </div>
        `;
    },
};

export default markupCreators;