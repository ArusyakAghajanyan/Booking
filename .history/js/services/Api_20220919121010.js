class Api {
    static catchError(e) {
        console.log(e);
    }

    static objectToArray(obj) {
        return Object.entries(obj).map((entry) => {

        })
    }
}
const arr = [1, 2];
export default Api;